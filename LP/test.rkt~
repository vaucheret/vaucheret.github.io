	    (define car-if-pair
	      (lambda (item)
		(cond
		 ((pair? item) (car item))
		 (else item))
		)
	      )

	  (define singleton-list?
	     (lambda (ls)
	       (and (pair? ls) (null? (cdr ls)))
	       )
	     )
